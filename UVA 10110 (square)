#include <iostream>
#include<cmath>
using namespace std;

int main() {
	unsigned long long n;
	while (cin >> n) {
		if (n == 0)
			break;
		unsigned long long t = sqrt(n);
		if (n == t*t) cout << "yes\n";
		else cout << "no\n";
	}
	return 0;
}
